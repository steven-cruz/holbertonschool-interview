#ifndef SORT_H
#define SORT_H

#include <stddef.h>
#include <stdlib.h>

/* Print Function */

void print_array(const int *array, size_t size);

/* Sort Function */

void merge_sort(int *array, size_t size);

#endif /* SORT_H */
